# Motor Imagery BCI (MATLAB)

This repository aims to implement some state-of-the-art DL models for classification of EEG-based motor imagery tasks in MATLAB.

Each folder in `src` is a seperate model.
